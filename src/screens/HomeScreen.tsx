import React from 'react';
import {ScrollView, Text} from 'react-native';

const HomeScreen = () => {
  return (
    <ScrollView>
      <Text>Test</Text>
    </ScrollView>
  );
};

export default HomeScreen;
